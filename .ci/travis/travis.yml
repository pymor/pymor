# THIS FILE IS AUTOGENERATED -- DO NOT EDIT #
#   Edit and Re-run .travis.yml.py instead  #


sudo: false
dist: trusty
language: bash
services: docker

# necessary for version.py generation to function properly
git:
    depth: 1000

before_script:
    - export IMAGE="pymor/testing:${DOCKER_TAG}"
    - docker pull ${IMAGE}
    - export ENV_FILE=${HOME}/env
    - printenv | \grep TRAVIS > ${ENV_FILE}
    - printenv | \grep PYTEST_MARKER >> ${ENV_FILE}
    - export DOCKER_RUN="docker run -e LOCAL_USER_ID=$(id -u) --env-file ${ENV_FILE} -v ${TRAVIS_BUILD_DIR}:/src ${IMAGE}"

script:
    - ${DOCKER_RUN} /src/.ci/travis/script.bash

# runs independent of 'script' failure/success
#after_script:
    #- ${DOCKER_RUN} /src/.ci/travis/after_script.bash

notifications:
  email:
    on_success: change
    on_failure: change
    on_start: never

branches:
  except:
    - gh-pages

matrix:
  include:
    - env: PYTEST_MARKER="None" DOCKER_TAG="2.7"
    - env: PYTEST_MARKER="PIP_ONLY" DOCKER_TAG="2.7"
    - env: PYTEST_MARKER="MPI" DOCKER_TAG="2.7"
    - env: PYTEST_MARKER="None" DOCKER_TAG="3.4"
    - env: PYTEST_MARKER="PIP_ONLY" DOCKER_TAG="3.4"
    - env: PYTEST_MARKER="MPI" DOCKER_TAG="3.4"
    - env: PYTEST_MARKER="None" DOCKER_TAG="3.5"
    - env: PYTEST_MARKER="PIP_ONLY" DOCKER_TAG="3.5"
    - env: PYTEST_MARKER="MPI" DOCKER_TAG="3.5"
    - env: PYTEST_MARKER="None" DOCKER_TAG="3.6"
    - env: PYTEST_MARKER="PIP_ONLY" DOCKER_TAG="3.6"
    - env: PYTEST_MARKER="MPI" DOCKER_TAG="3.6"

# THIS FILE IS AUTOGENERATED -- DO NOT EDIT #
#       Re-run .travis.yml.py instead       #